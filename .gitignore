# macOS
.DS_Store

# Claude Code local state (project agents/ and shared settings.json ARE tracked)
.claude/settings.local.json

# Local git worktrees (using-git-worktrees skill)
.worktrees/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# env files (keep .env.example tracked)
.env
.env.local

# UV package manager
uv.lock

# agctl test-runbook results (ephemeral per-run reports)
*.results.md

# agctl mock daemon runtime state (pidfiles + logs; default state-dir ./.agctl/)
.agctl/

# superpowers SDD scratch (task briefs/reports, review packages, progress ledger)
.superpowers/
